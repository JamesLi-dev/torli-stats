# Torli Stats 待实现清单

> 本文件用于记录后续产品、设置界面和工程优化方向。`docs/` 为本地规划资料目录，不纳入版本提交。

## 当前分支已处理

- [x] 仅为显示在面板或状态栏的 Codex 账号创建刷新 Store，隐藏账号不再自动刷新。
- [x] 风扇 RPM 缺失时不再停止 CPU/GPU 温度的后续传感器读取。
- [x] 将更新间隔统一收口到 `AppSettings.refreshInterval`，设置界面直接绑定配置。
- [x] 新增传感器设置区域：辅助进程状态、风扇/CPU 温度/GPU 温度能力、最近读取时间、重新检测、重新安装和卸载。
- [x] Dashboard 的 Codex 账号默认展示前 2 个，其余账号按需展开。
- [x] Codex 剩余额度按阈值使用绿色、橙色和红色提示。
- [x] 新增状态栏 Logo 设置：可关闭或启用随 CPU 加速的 RunCatNeo / RunnerGallery 动效，内置 9 种 Apache-2.0 动画并保留归属说明。

## 第一优先级：稳定性与基础体验

- [ ] Dashboard 设置最大高度并支持垂直滚动，避免多模块、多账号、15 个进程时超出屏幕。
- [ ] 进程列表支持单独滚动或限制展示高度。
- [ ] 增加基础自动化测试：CPU、网络速率、Codex JSON、SMC 解码、设置默认值和状态栏排序。
- [ ] 处理 CPU 计数器重置、网络计数器回退等边界情况。
- [ ] 为 Codex 增加请求取消、超时后的重试/退避和过期数据提示。
- [ ] 为恢复默认设置增加确认弹窗，并明确 Codex 本地登录目录是否保留。

## 设置界面

### 通用与监控

- [ ] 接通电源和使用电池时使用不同更新间隔。
- [ ] 低电量时自动开启省电模式。
- [ ] 增加“仅打开面板时刷新”和“后台持续监控”选项。
- [ ] 支持暂停监控。
- [ ] 支持显示或隐藏不可用模块。
- [ ] 增加数据单位设置：KB/s、MB/s、Mbps；整数或一位小数。

### 状态栏

- [ ] 支持指标拖拽排序，而不只是上下移动。
- [ ] 支持显示/隐藏图标、调整字体大小。
- [ ] 支持 CPU/内存高占用颜色阈值。
- [ ] Codex 账号过多时支持汇总、限制数量或使用省略号。
- [ ] 支持自定义 Codex 账号短名称，避免邮箱前缀重复。
- [ ] 明确 Codex 总开关、默认账号和单个账号状态栏开关之间的关系。

### Dashboard 面板

- [ ] 支持面板模块排序。
- [ ] 增加紧凑、标准、详细三种密度。
- [ ] 增加图表时间范围：1 分钟、5 分钟、30 分钟、1 小时。
- [ ] 允许单独隐藏温度标签、设备信息和进程 PID。
- [ ] 允许进程列表只显示 CPU、只显示内存或同时显示。
- [ ] 电池电量和健康度根据阈值显示不同颜色。
- [x] Codex 额度接近耗尽时恢复橙色/红色警告，而不是始终绿色。

### 传感器

- [ ] 显示辅助进程版本和上次成功读取时间。
- [ ] 显示每类传感器的可用/不可用原因。
- [ ] 增加辅助进程安装失败的详细诊断信息。
- [ ] 增加复制诊断信息功能。
- [ ] 检查辅助进程文件签名和版本，避免旧版本 helper 与新 App 不匹配。
- [ ] 评估是否需要在 App 退出或卸载时清理辅助进程。

### Codex 账号

- [ ] 自动刷新开关和自动刷新间隔。
- [ ] 打开面板时刷新。
- [ ] 增加账号连接测试和上次成功刷新时间。
- [ ] 选择 Codex Home 后立即验证目录、`auth.json` 和 Codex CLI。
- [ ] 支持导入已有 Codex Home，而不是只能创建托管目录。
- [ ] 显示 Credits、周额度、额度重置时间和登录失效状态。
- [ ] 账号删除增加确认，并明确“只移除配置、不删除登录态”。

### 系统与维护

- [ ] 设置导入/导出。
- [ ] 设置版本迁移机制。
- [ ] 增加通知开关和隐私说明。
- [ ] 增加版本号、检查更新、打开日志目录和关于页面。
- [ ] 恢复默认设置时可选择是否清理托管 Codex 目录。

## 功能扩展

### 通知与告警

- [ ] CPU 持续超过阈值时通知。
- [ ] 内存压力或使用率超过阈值时通知。
- [ ] 磁盘剩余空间过低时通知。
- [ ] CPU/GPU 温度过高时通知。
- [ ] 电池健康度或电量过低时通知。
- [ ] Codex 剩余额度低于阈值时通知。
- [ ] 支持“持续多少秒后通知”，避免瞬时尖峰造成打扰。

### 历史数据

- [ ] 增加 CPU、内存、网络、GPU 的环形历史缓存。
- [ ] 支持最近 1 分钟、5 分钟、1 小时和 1 天趋势。
- [ ] 增加最大值、最小值和平均值展示。
- [ ] 后续再评估是否需要落盘保存历史数据。

### 更多系统指标

- [ ] Swap 使用量。
- [ ] 内存压力。
- [ ] CPU Load Average。
- [ ] 系统热状态。
- [ ] 各磁盘卷使用率。
- [ ] 各网络接口速率。
- [ ] GPU 显存/共享内存。
- [ ] 低电量模式状态。
- [ ] 更丰富的电源适配器信息。

### 快捷操作

- [ ] 右键菜单增加立即刷新。
- [ ] 右键菜单增加暂停监控。
- [ ] 支持复制当前状态摘要。
- [ ] 支持直接打开设置、日志目录和关于页面。
- [ ] 增加检查更新入口。

## 性能与工程质量

- [ ] GPU 仅在相关模块或状态栏启用时读取，省电模式下进一步降低 `ioreg` 频率。
- [ ] 缓存硬件信息和系统信息，减少 `system_profiler` 启动次数。
- [ ] 将指标采样周期配置化，避免多个定时器策略分散在 Store 中。
- [ ] 持续检查后台队列和主线程之间的数据竞争。
- [ ] 将设置模型拆分为通用、状态栏、Dashboard、Codex、传感器等子配置。
- [ ] 增加 SwiftLint 或等效静态检查。
- [ ] 增加硬件兼容性手工测试记录：Intel、不同 Apple Silicon、台式 Mac、无风扇设备。

## 发布与分发

- [ ] 构建 Universal Binary：arm64 + x86_64。
- [ ] 完善 Developer ID 签名和公证流程。
- [ ] 增加自动更新能力。
- [ ] 增加完整卸载脚本，包含 App、LaunchDaemon、PrivilegedHelperTools 和配置说明。
- [ ] CI 中运行测试、检查签名、公证和产物架构。
- [ ] 保持中英文界面和 README 同步，逐步接入本地化资源。
- [ ] 补充 VoiceOver、键盘导航和图表辅助标签。

## 推荐后续顺序

1. Dashboard 高度/滚动和 Codex 过期状态。
2. Codex 自动刷新设置、Home 验证和账号连接测试。
3. 传感器诊断信息、helper 版本检查和硬件兼容性测试。
4. 阈值通知。
5. 历史趋势。
6. Universal Binary、签名公证和自动更新。
